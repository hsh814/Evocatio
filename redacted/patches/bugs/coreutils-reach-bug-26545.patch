diff --git a/src/shred.c b/src/shred.c
index a317c44393ab..f9fa45480211 100644
--- a/src/shred.c
+++ b/src/shred.c
@@ -287,6 +287,7 @@ fillpattern (int type, unsigned char *r, size_t size)
   r[0] = (bits >> 4) & 255;
   r[1] = (bits >> 8) & 255;
   r[2] = bits & 255;
+  abort();
   for (i = 3; i < size / 2; i *= 2)
     memcpy (r + i, r, i);
   if (i < size)
